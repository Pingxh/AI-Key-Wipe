# AI-Key-Wipe 产品文档

> **版本**: 1.0.0  
> **最后更新**: 2026-07-30  
> **平台**: macOS 13.0+ / Windows 10+ / Linux

---

## 1. 产品概述

### 1.1 产品定位

AI-Key-Wipe 是一款面向开发者的 **跨平台 AI 密钥隐私清理工具**。它帮助用户快速发现并清除本地存储的 AI API Key、令牌、认证凭据等敏感信息，适用于设备交接、隐私自查、环境重置等场景。

### 1.2 核心价值

| 维度 | 说明 |
|------|------|
| **隐私保护** | 一键清除散落在系统各处的 AI 凭据，防止泄露 |
| **跨平台** | 同一套代码支持 macOS / Windows / Linux |
| **灵活定制** | 自定义扫描路径和匹配模式，不限于预设 |
| **所见即所得** | 选中即清除，精准控制清除范围 |

### 1.3 目标用户

- **AI 开发者**：本地配置了多个 LLM API Key（OpenRouter、DeepSeek 等）
- **使用 AI 工具的用户**：Claude Desktop、GitHub Copilot、Hermes Agent 等
- **需要交接设备的人员**：出售/转赠电脑前擦除个人凭据

---

## 2. 产品架构

```
┌─────────────────────────────────────────────────────┐
│                    KeyClean App                       │
│  ┌─────────────────────────────────────────────────┐ │
│  │             HTML/CSS/JS Frontend                  │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────────┐  │ │
│  │  │ 目标列表  │  │ 目标详情  │  │ 结果面板     │  │ │
│  │  │ (多选)   │  │ (编辑)   │  │ (日志)      │  │ │
│  │  └────┬─────┘  └────┬─────┘  └──────┬───────┘  │ │
│  └───────┼──────────────┼───────────────┼──────────┘ │
│          ▼              ▼               ▼            │
│  ┌─────────────────────────────────────────────────┐ │
│  │              Rust / Tauri Backend                 │ │
│  │  ┌──────────────────┐  ┌──────────────────────┐ │ │
│  │  │   storage (JSON)  │  │     wipe_service     │ │ │
│  │  │  (持久化/CRUD)    │  │  (扫描/清除/匹配)    │ │ │
│  │  └──────────────────┘  └──────────────────────┘ │ │
│  └─────────────────────────────────────────────────┘ │
│  ┌─────────────────────────────────────────────────┐ │
│  │            System APIs (via Tauri)                │ │
│  │  ├── 文件系统  ├── 原生对话框  ├── 系统托盘      │ │
│  │  └── WebView2/WebKit    └── 菜单栏              │ │
│  └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

### 2.1 技术栈

| 组件 | 技术 |
|------|------|
| 前端 | HTML + CSS + JS (原生，无框架) |
| 后端 | Rust + Tauri v2 |
| 序列化 | serde + serde_json |
| 正则匹配 | regex crate |
| 文件对话框 | rfd crate |
| 持久化 | JSON 文件 (~/.config/com.keyclean/) |
| 打包 | dmg (macOS) / msi (Windows) / deb (Linux) |
| CI | GitHub Actions (3 平台自动构建) |

### 2.2 模块职责

| 模块 | 文件 | 职责 |
|------|------|------|
| App 入口 | `main.rs` / `lib.rs` | 启动入口、Tauri 命令注册 |
| 主界面 | `index.html` + `app.js` + `style.css` | 左侧列表 + 右侧详情 + 操作栏 |
| 数据模型 | `models.rs` | WipeTarget / WipeResult / ScannedFile |
| 配置管理 | `storage.rs` | JSON 持久化、路径检查 |
| 清除引擎 | `wipe_service.rs` | 文件扫描、匹配模式构建、清除执行 |
| 构建配置 | `tauri.conf.json` / `Cargo.toml` | 项目配置、依赖管理 |

---

## 3. 核心功能清单

### 3.1 目标管理

| 功能 | 说明 |
|------|------|
| 自动扫描 | 递归扫描家目录，自动发现含 API Key 的文件 |
| 自定义路径添加 | 通过系统文件选择器选择任意文件 |
| 多选操作 | 复选框多选 |
| 目标编辑 | 修改名称、重新选择路径、启用/禁用 |
| 自定义匹配模式 | 每目标独立配置额外匹配关键词 |
| 列表删除 | 选中后按 Delete 键或点击「移出列表」 |
| 自动移除 | 清除成功后自动从列表删除（可开关） |

### 3.2 扫描与清除

| 功能 | 说明 |
|------|------|
| 递归扫描 | 自动发现所有子目录中的配置文件（深度 4-5 层） |
| 实时进度 | 前端显示当前扫描到的目录 |
| 平台适配 | Windows 扫描 Desktop/Documents，macOS 跳过系统目录 |
| 单文件清除 | 选中单个目标后清除 |
| 批量清除 | 选中多个目标后批量清除 |
| 智能格式识别 | 自动识别 .env、YAML、export 等格式 |
| 多行值处理 | 处理 YAML 多行值（`key:\n  value`） |

### 3.3 安全机制

| 功能 | 说明 |
|------|------|
| 操作确认 | 执行前显示确认对话框 |
| 值清除而非注释 | 清空密钥值（`KEY=xxx` → `KEY=`），不保留原文 |
| 格式保留 | 保留文件结构，仅清空敏感值 |

---

## 4. 扫描策略

### 4.1 跨平台差异

| 维度 | macOS | Windows | Linux |
|------|-------|---------|-------|
| 最大深度 | 4 层 | 5 层 | 4 层 |
| 跳过 Desktop | 是 | 否 | 是 |
| 跳过 Documents | 是 | 否 | 是 |
| 跳过 Downloads | 是 | 否 | 是 |
| 额外路径 | — | AppData\Roaming\ | — |
| 隐藏目录 | 跳过 (除 .ssh/.aws 等) | 跳过 | 跳过 |

### 4.2 设计原因

Windows 用户的项目文件通常散落在 Desktop、Documents 等目录中，且目录层级较深（`Desktop\work\project-2024\backend\.env`）。因此 Windows 的扫描深度更深（5 层），且不跳过这些常用目录。

macOS 用户的配置文件通常直接在 `~/` 根目录（`.env`、`.zshrc`）或 `~/Projects/` 下，深度较浅，因此跳过 Desktop/Documents 等系统目录以提升性能。

---

## 5. 项目文件结构

```
AI-Key-Wipe/
├── AIKeyWipe-Tauri/              # Tauri 跨平台项目
│   ├── src/                      # 前端
│   │   ├── index.html            # 主界面
│   │   ├── app.js                # 前端逻辑
│   │   └── style.css             # 样式
│   ├── src-tauri/                # Rust 后端
│   │   ├── src/
│   │   │   ├── main.rs           # 入口
│   │   │   ├── lib.rs            # Tauri 命令注册
│   │   │   ├── models.rs         # 数据模型
│   │   │   ├── wipe_service.rs   # 扫描/清除引擎
│   │   │   └── storage.rs        # 持久化
│   │   ├── Cargo.toml            # Rust 依赖
│   │   └── tauri.conf.json       # Tauri 配置
│   ├── BUILD.md                  # 构建文档
│   └── build.sh                  # 本地构建脚本
├── .github/workflows/            # CI 配置
│   └── release.yml               # 3 平台自动构建
├── README.md                     # 快速上手
├── PRODUCT.md                    # 产品文档
├── FEATURES.md                   # 功能文档
├── AIKeyWipe/                    # (旧版) Swift 项目
└── ai-key-wipe.sh                # (旧版) CLI 脚本
```

---

## 6. 系统要求

| 平台 | 最低版本 | 架构 |
|------|---------|------|
| macOS | 13.0 (Ventura) | Apple Silicon + Intel |
| Windows | 10 | x86_64 |
| Linux | 见 Tauri 要求 | x86_64 |

---

## 7. 路线图

- [x] macOS 支持
- [x] Windows 支持
- [x] 递归扫描
- [x] 平台适配的跳过目录
- [x] 实时扫描进度
- [ ] Linux 支持（CI 已配置，待测试）
- [ ] 拖拽文件添加到列表
- [ ] 清除前预览匹配结果
- [ ] 备份模式（清除前先备份）